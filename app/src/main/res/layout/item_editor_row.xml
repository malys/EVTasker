<?xml version="1.0" encoding="utf-8"?>
<!-- One row for both conditions AND actions: both reduce to "label — value", and the
     "when stopped only" badge is the only difference. -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/spacing_sm"
    app:cardBackgroundColor="@color/mg4_surface_raised"
    app:cardCornerRadius="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:minHeight="@dimen/touch_target"
        android:paddingStart="@dimen/spacing_md"
        android:paddingEnd="@dimen/spacing_sm">

        <LinearLayout
            android:id="@+id/rowClickArea"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:minHeight="@dimen/touch_target"
            android:gravity="center_vertical"
            android:paddingVertical="@dimen/spacing_sm"
            android:focusable="true"
            android:clickable="true">

            <TextView
                android:id="@+id/rowLabel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAppearance="@style/Text.MG4.Body" />

            <TextView
                android:id="@+id/rowGated"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_xs"
                android:text="@string/editor_gated_hint"
                android:textAppearance="@style/Text.MG4.Secondary"
                android:textColor="@color/mg4_warn"
                android:visibility="gone" />
        </LinearLayout>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/rowRemove"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minHeight="@dimen/touch_target"
            android:text="@string/editor_remove" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
