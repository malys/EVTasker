<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ev_background"
    android:orientation="vertical"
    android:padding="@dimen/spacing_lg">

    <!-- A full-screen value picker is still an editor screen: its escape and commit
         actions stay in the same top-right position as the rule and branch editors. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/editorTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="@dimen/spacing_lg"
            android:maxLines="1"
            android:ellipsize="end"
            android:textAppearance="@style/Text.EV.Display" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/editorCancel"
            style="@style/Widget.EV.Button.Outlined"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/spacing_sm"
            android:text="@string/editor_cancel" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/editorSave"
            style="@style/Widget.EV.Button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/value_ok" />
    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/spacing_md"
        android:text="@string/value_editor_instruction"
        android:textAppearance="@style/Text.EV.Secondary" />

    <include
        android:id="@+id/valueEditorContent"
        layout="@layout/dialog_value_editor"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
